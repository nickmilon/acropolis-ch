
/**
 * module 1 description
 * this is a module
 * @module
 */

/**
 * multiplies
 *
 * @param {number} p1 a xxx
 * @param {number} p2 a yyyy
 * @return {number} a number rrrrrrrr
 * @example
 * myFunction(1,2) => 3
 */
export function myFunction(p1, p2) {
  return p1 * p2;   // The function returns the product of p1 and p2
}

/**
 * multiplies
 * @param {number} p1 a xxx
 * @param {number} p2 a yyyy
 * @return {number} a number rrrrrrrr
 * @example
 * multi(1,2) => 3
 */
export const multi = (p1, p2) => p1 * p2;
